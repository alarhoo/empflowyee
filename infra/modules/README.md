# Infrastructure modules

Likely Terraform modules will include Artifact Registry, Workload Identity, service accounts, Cloud Run, Secret Manager, Cloud SQL, networking and observability.

Do not create generic modules before the first real environment implementation proves the abstraction.
