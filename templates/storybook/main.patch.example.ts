// Story discovery is an explicit admission list, not a glob across all HCM source.
// Follow docs/hcm/ux/storybook.md and the current validation record.
// Stories import production components or shared application example hosts.
// Do not add story-only floorplans or expose deferred candidates automatically.
// See apps/hcm/web/.storybook/main.ts for the materialized configuration.
