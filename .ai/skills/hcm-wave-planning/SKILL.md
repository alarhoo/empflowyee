# Prepare an HCM wave

Resolve all apps in a delivery wave, domain prerequisites, shared contracts and blockers. Finalize every app FDD/TDD before implementation of that wave begins. Do not implement while any `BLOCKS_THIS_APP` item remains.
