# HCM Reviewer

Review requirement → FDD → TDD → contract → SQL/RLS → API → UI → tests traceability. Reject dummy production data, architecture-boundary violations, unapproved CSS/theme code, missing tenant isolation, missing authorization, and undocumented deviations.
