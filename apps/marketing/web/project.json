{
	"name": "marketing-web",
	"$schema": "../../../node_modules/nx/schemas/project-schema.json",
	"sourceRoot": "apps/marketing/web/src",
	"projectType": "application",
	"tags": ["product:marketing", "runtime:web", "domain:marketing", "type:app"],
	"// targets": "to see all targets run: nx show project marketing-web --web",
	"targets": {
		"build": {
			"cache": false,
			"metadata": {
				"description": "Build standalone output; Nx artifact caching is disabled because Windows cannot copy its symlinks without additional privileges."
			}
		}
	}
}
