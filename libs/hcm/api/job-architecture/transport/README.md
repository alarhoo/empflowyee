# HCM job architecture transport

HTTP adapters of the job architecture domain (`hcm-api-job-architecture-transport`): the Job
Catalogue controller at `/api/v1/job-architecture`. Validation belongs to the contract and the
use cases; the controller only routes verified requests.
