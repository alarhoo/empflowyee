# HCM employee module

Global Nest module (`hcm-api-employee-module`) that provides `EmployeePortBinder` and the
`OrgChartFieldPolicyBinder` declared by workforce-foundation. The HCM API root imports it once.
Database acceptance for the profile policy foundation lives here.
