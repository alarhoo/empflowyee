# HCM employee infrastructure

Kysely adapters of the employee domain (`hcm-api-employee-infrastructure`): the profile policy
reader over the tables of migration `000022`, the employee port binder, and the employee
implementation of the org chart's `OrgChartFieldPolicyBinder`.
