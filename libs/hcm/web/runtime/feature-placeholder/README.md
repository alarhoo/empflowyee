# hcm-web-runtime-feature-placeholder

Native Standard Page used to prove catalog-driven lazy route matching. It contains no employee data, forms or business behavior. Only the HCM app composition root imports it lazily after the runtime guard succeeds.

See the [shell maintainer guide](../../../../../docs/hcm/architecture/shell/README.md). The `hcm-web` build and browser suite verify the feature.
