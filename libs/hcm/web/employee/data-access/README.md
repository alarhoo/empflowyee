# HCM employee data-access

Browser data access for the employee domain. It calls the tenant-scoped `/api/v1/employee` API
through the runtime-universal `hcm-employee-contract` DTOs; it holds no fixture data.
