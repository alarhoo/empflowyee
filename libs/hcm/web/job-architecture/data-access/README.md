# HCM job architecture web data access

Angular data access of the job architecture domain (`hcm-web-job-architecture-data-access`):
`JobCatalogueApi` over `/api/v1/job-architecture` with retained idempotency keys, and the
translation of stable server classifications into user messages.
